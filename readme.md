# Zingularity

Zingularity is a vscode extension for the Z80 assembly programmer, specifically using the z88dk assembler.

## Language Server

It implements a real language server (because) and parses the assembler file content to give you a rich set of information that allows the more advanced features that other Z80 extensions do not provide.
